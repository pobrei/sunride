declare module 'leaflet.heat' {
  // This is a placeholder declaration file for the leaflet.heat module
  // It doesn't provide any specific type information but allows TypeScript to recognize the module
}
